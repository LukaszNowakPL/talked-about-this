export enum APP_ROUTES {
  COUNTRIES = "/countries",
  AIRPORTS = "/airports",
  AIRPORT = "/airports/:id",
  ADD_AIRPORT = "/airports/add",
}

export enum QUERIES {
  COUNTRIES = "countries",
  AIRPORTS = 'airports',
  AIRPORT = 'airport',
}
